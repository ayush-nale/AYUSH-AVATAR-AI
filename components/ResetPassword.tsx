"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
export default function ResetPassword(){const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const router=useRouter();return <main className="shell" style={{display:"grid",placeItems:"center",padding:20}}><form className="glass shadow" onSubmit={async e=>{e.preventDefault();setMsg("");const {error}=await createClient().auth.updateUser({password});if(error)setMsg(error.message);else{setMsg("Password updated.");setTimeout(()=>router.replace("/auth/login"),800)}}} style={{width:"min(460px,100%)",padding:28,borderRadius:28}}><h1>Choose a new password</h1><input className="focus-ring" required minLength={8} type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)} style={{width:"100%",padding:12,borderRadius:14,background:"#08131c",color:"var(--text)",border:"1px solid var(--line)"}}/><button className="focus-ring" style={{marginTop:14,width:"100%",padding:12,borderRadius:14,border:0,background:"var(--accent)",fontWeight:800}}>Update password</button>{msg&&<p role="status" style={{color:"var(--muted)"}}>{msg}</p>}</form></main>}
